#头文件
#include "stm32f1xx_hal.h"
#include "gpio.h"
修改添加

//引脚定义配置
void OLED_W_SCL(uint8_t BitValue){
	GPIO_WriteBit(GPIOB, GPIO_Pin_8, (BitAction)BitValue);
}
--修改
void OLED_W_SCL(GPIO_PinState x){
	HAL_GPIO_WritePin(GPIOB, GPIO_PIN_8, x);
}
改为使用HAL库已配置好的GPIO

OLED_GPIO_Init中移除相关引脚配置代码
MX_GPIO_Init();    //添加HAL库对GPIO的初始化函数
	/*释放SCL和SDA*/
OLED_W_SCL(GPIO_PIN_SET);
OLED_W_SDA(GPIO_PIN_SET);
/* 注意：这里要将所有的关于已定义的GPIO口所需要的上下拉定义为GPIO_PIN_SET/GPIO_PIN_RESET 否则会有warning

对于使用到TIM定时器的程序，需要在CubeMX中设置好引脚以及定时器通道
！！！设置 Prescaler 和 Counter Period
对于TIM3定时中断来说 要实现1ms中断一次就需要 Prescaler = 72-1 / Counter Period = 1000-1
对于TIM1使用 PWM Generation 产生PWM波需要 Prescaler = 100-1 / Counter Period = 36-1 （否则无法正确产生PWM波驱动电机 --实测！）
